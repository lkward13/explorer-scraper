# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["python311", "playwright-driver-chromium"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "playwright install chromium",
    "playwright install-deps chromium"
]

[start]
cmd = "python worker/run_daily.py"

